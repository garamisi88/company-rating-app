<span class="stars"
      style="--rating: {{ rating }}"
      role="img"
      aria-label="{{ 'review.rating_aria'|trans({'%rating%': rating|number_format(1, ',', ' ')}) }}">
    <span class="stars__filled"></span>
</span>
